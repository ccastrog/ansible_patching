---
- name: Parcha un conjunto de servidores linux
  become: yes
  hosts: all
  tasks:
    - name: Ejecuta comando para actualizar paquetes
      ansible.builtin.dnf:
        name: "*"
        state: latest
